# 인수인계 문서

새 대화에서 이 작업을 이어받을 때 먼저 읽는 문서입니다.
이 zip 을 올리고 "인수인계.md 를 읽고 이어서 작업해 줘" 라고 하시면 됩니다.

---

## 1. 무엇을 하고 있나

학부 구조해석 강의에서 쓸 **Stabileo** 를 학생 PC 에서 오프라인으로
실행할 수 있게 만드는 작업입니다.

- 원본: https://github.com/lambdaclass/stabileo (AGPL-3.0)
- 내 저장소: https://github.com/lghlsk/stabileo-course
- 리눅스 서버 한 대에서 Windows · macOS · Linux 배포본을 한꺼번에 생성
- 학생은 설치·인터넷·계정 없이 실행. 해석은 각자 브라우저에서 WASM 으로 수행

Stabileo 는 Rust 솔버를 WebAssembly 로 컴파일해 브라우저에서 돌리는
구조해석 도구입니다. 서버로 해석 요청을 보내지 않으므로 동시 접속자가
늘어도 서버 부담이 없습니다.

---

## 2. 빌드 환경 (확인된 것)

| | |
|---|---|
| OS | Ubuntu 24.04 LTS |
| node | v18.19.1 |
| go | 1.22.2 |
| Rust | **rustup 으로 설치** (apt 아님 — 아래 §5 참조) |
| llvm-lipo | llvm-lipo-18 (macOS 유니버설 바이너리용) |
| 고정 커밋 | `b1cc418ef424` (2026-09-10, PR #186) |

고정 커밋은 **WASM 캐시를 만든 커밋과 반드시 같아야** 합니다. 캐시에
`COMMIT` 표식이 들어 있고 빌드 스크립트가 대조해 다르면 멈춥니다.
캐시를 새로 뜨면 이 표와 아래 §3 의 명령도 함께 고치세요.

빌드 시간: WASM 캐시가 있으면 전체 2~3분. 병목은 vite 번들링(약 70초)
이며 CPU 코어 수에만 좌우됩니다. npm 설치는 7초로 무관합니다.

---

## 3. 사용법

```bash
# 첫 빌드 (Rust 필요, 10~20분)
./build-stabileo.sh --wasm-cache ~/stabileo-wasm --keep

# 이후 (Rust 불필요, 2~3분)
./build-stabileo.sh --wasm-cache ~/stabileo-wasm --commit b1cc418ef424

# GitHub 에 올리기
./publish.sh --dry-run
./publish.sh
```

주요 옵션: `--os win,mac,linux` · `--commit` · `--wasm` · `--wasm-cache`
· `--no-wasm` · `--sign` (macOS 호스트) · `--no-patches` · `--keep`

`--wasm`, `--wasm-cache`, `--no-wasm` 중 하나는 **반드시** 골라야 합니다.
고르지 않으면 스크립트가 시작하지 않습니다 (§5.14).

`build-stabileo.sh` 는 **같은 폴더의 `apply-*.sh` 를 이름 순으로 모두
자동 적용**한 뒤 빌드합니다. 새 수정이 필요하면 `apply-*.sh` 를 하나 더
만들어 넣으면 됩니다. 저장소를 매번 새로 클론하므로 소스 수정은 반드시
이 형태여야 합니다.

---

## 4. 파일 목록

### 빌드
| 파일 | 역할 |
|---|---|
| `build-stabileo.sh` | 클론 → 패치 → WASM → 빌드 → 3개 OS 패키징 → 압축 |

### 패치 (빌드 시 자동 적용, 이름 순)
| 파일 | 역할 |
|---|---|
| `apply-dsm-fix.sh` | 단계별 강성법 NaN 버그 수정 + 회귀 테스트 |
| `apply-dz-naming.sh` | 2D 지점 강제변위 표기를 `dz` 로 통일 |
| `apply-korean.sh` | 한국어 활성화 + `ko-extra.ts` 병합 |
| `apply-light-png.sh` | PNG 내보내기를 밝은 배경으로 |
| `apply-multibody.sh` | 분리된 구조물 해석 허용 (성분별 안정성 검사) |
| `apply-text-io.sh` | 텍스트 형식 입출력 UI 추가 |

### 패치가 쓰는 원본
| 파일 | 역할 |
|---|---|
| `ko-extra.ts` | 한국어 보충 번역 186개 |
| `text-format.ts` | 텍스트 형식 인코더/디코더 (앱 내장용) |
| `textio-strings.tsv` | 위 파일의 문구 71개 × en·es·pt·ko |
| `multibody-strings.tsv` | 분리 구조물 검사 문구 3개 × en·es·pt·ko |
| `light-png.ts` | 명도 반전 변환 |

### 도구
| 파일 | 역할 |
|---|---|
| `dedtext.py` | 명령행 `.ded` ↔ `.txt` 변환기 |
| `check-textio-i18n.py` | `textio-strings.tsv` ↔ `text-format.ts` 정합성 검사 |
| `locale-insert.py` | 문구표를 네 사전에 넣는 공용 도구 |
| `publish.sh` | GitHub 업로드 (gh CLI) |
| `untrack.sh` | 잘못 올린 파일 빼내기 |
| `reset-history.sh` | 이력 전체를 지우고 다시 시작 |

### 상류 기여용 (`upstream/`)
| 파일 | 역할 |
|---|---|
| `prepare-pr.sh` | NaN 수정의 영어 주석판 |
| `COMMIT_MSG.txt` | 커밋 메시지 |
| `PR.md` | PR 제목·본문 |

---

## 5. 함정 모음 (여기가 이 문서의 핵심)

같은 곳에서 두 번 막히지 않도록, 실제로 겪은 것만 적었습니다.

### 5.1 WASM 은 선택이 아니라 필수

`wasm-solver.ts` 주석과 콘솔 메시지에 "JS 솔버로 폴백한다" 고 적혀
있으나 **JS 폴백 솔버는 존재하지 않습니다.** `solve()` 는 예외를
던집니다. 주석이 구현보다 뒤처진 것입니다.

WASM 없이 빌드하면 빌드는 성공하고 편집기도 열리지만 **해석 버튼을
누르는 순간 실패**합니다. 빌드 스크립트가 이제 결과물에 솔버 WASM 이
들어갔는지 자동 검증합니다.

### 5.2 Rust 는 반드시 rustup 으로

`engine/rust-toolchain.toml` 이 nightly 와 `rust-src` 를 요구합니다.
이 파일은 rustup 전용 규약이라 **apt 로 설치한 cargo 는 무시**하고
배포판 버전으로 빌드하다 `rustversion` / `wasm-bindgen` 에서 실패합니다.

```bash
sudo apt remove rustc cargo
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
source "$HOME/.cargo/env"
cargo install wasm-pack
```

### 5.3 호스트 링커 (clang + mold)

`engine/.cargo/config.toml` 이 호스트 타깃 링커를 `clang` + `mold` 로
지정합니다. WASM 과 무관해 보이지만 **빌드 스크립트와 proc-macro
크레이트가 호스트용으로 컴파일**되므로 링커가 없으면 전부 실패합니다.

빌드 스크립트가 `clang`/`mold` 유무를 확인하고, 없으면 해당 섹션만
제거합니다(`config.toml.orig` 로 백업). `[unstable]` 의 `build-std` 는
**반드시 보존**해야 합니다. WASM 에서 `Instant::now()` 패닉을 막는
설정이라 지우면 해석 중 런타임 오류가 납니다.

### 5.4 macOS Gatekeeper

서명 없는 앱은 실행이 막힙니다. 배포본에 `처음 한 번만 실행.command`
(격리 속성 해제) 를 넣었습니다. **macOS 15 Sequoia 부터 우클릭 → 열기
방식이 막혔으므로** 시스템 설정 → 개인정보 보호 및 보안 경로도 안내에
포함했습니다.

Apple Silicon 은 임시 서명조차 없으면 실행되지 않습니다. 리눅스에서
`llvm-lipo` 없이 빌드하면 Intel 단일 빌드가 되어 Rosetta 경고가 뜨므로,
지금은 lipo 가 없으면 arm64 / Intel 을 **별도 패키지로** 만듭니다.
맥에서 빌드하면 `codesign` 으로 임시 서명이 자동 적용됩니다.

### 5.5 Windows 실행 껍데기

- `dist/index.html` 더블클릭은 안 됩니다. 자산 경로가 루트 절대경로라
  반드시 HTTP 로 서빙해야 합니다 (Go 로 만든 `server.exe` 동봉).
- 윈도는 MIME 을 레지스트리에서 읽으므로 `.wasm` 타입을 명시 등록해야
  합니다. 아니면 WASM 로딩이 실패합니다.
- `build:only` 는 프리렌더를 건너뛰므로 `/pro`, `/educativo` 가 파일로
  존재하지 않습니다. 서버에 SPA 폴백이 필요합니다.
- 배치 파일은 CRLF 여야 합니다.

### 5.6 3D 화면이 빈 경우

원 사이트에서도 같다면 **WebGL 문제**입니다. 메인 3D 뷰포트는
`new THREE.WebGLRenderer()` 를 try/catch 없이 호출해서, 컨텍스트 생성에
실패하면 안내 없이 빈 화면만 남습니다(철근 뷰어에는 가드가 있음).

`chrome://gpu` 의 WebGL 항목을 확인하고, 하드웨어 가속을 켜거나
`--enable-unsafe-swiftshader` 로 소프트웨어 렌더링을 강제하면 됩니다.

### 5.7 한국어

`ko.ts` 는 이미 3,149개 키가 번역되어 있으나 `OFFERED_LOCALES` 가
`['es','en','pt']` 로 좁혀져 **번들에 포함조차 되지 않습니다.** 개발팀이
불완전한 번역을 내보내지 않으려고 의도적으로 좁힌 것입니다.

- 영어 5,718 / 한국어 3,149 → 48% 누락
- 강의에서 쓰는 화면만 보면 2,110 중 670 누락 (32%)
- 그중 핵심 186개를 `ko-extra.ts` 로 채움
- **`dsm.*` 108개는 원본에 100% 번역되어 있음** (단계별 설명 화면)
- `locales/steel/` 에는 es·en·pt 만 있어 **철골 설계 198개 키는 영어**
- 누락분은 `tAt()` 이 영어로 자동 대체하므로 화면이 깨지지는 않음

### 5.8 `.ded` 파일

- 압축된 JSON. 확장자를 `.json` 으로 바꿔도 열림
- 저장 Ctrl+S(탭) / Ctrl+Shift+S(세션), 열기 Ctrl+O — **여는 명령은 하나**
  이고 단일/세션을 자동 판별
- 드래그 앤 드롭은 **DXF 만** 받음. `.ded` 는 열기 명령을 써야 함
- **파일명 생성 시 한글이 제거됨.** 모델 이름이 통째로 한글이면 전부
  기본 이름이 되므로, 학생에게 영문·숫자 이름을 쓰게 안내할 것
- 입력란에 커서가 있으면 단축키가 동작하지 않음

### 5.9 축과 단면 2차모멘트

- 저장 파일의 노드는 `x`, `y` 지만 **`y` 가 연직**입니다. 솔버 인터페이스는
  `x`, `z` 이고 하중이 `fx`/`fz`/`my` 인 것이 이를 확인해 줍니다.
- 면내 휨 강성에는 **`iy`(강축)** 를 씁니다 (`effectiveBendingInertia()`).
  `iz` 로 잘못 쓰면 IPN 300 기준 20배 넘게 달라집니다.

### 5.10 JavaScript `undefined` 직렬화

`JSON.stringify(undefined)` 는 문자열이 아니라 `undefined` 를 반환합니다.
템플릿 문자열에 넣으면 `"undefined"` 라는 글자가 파일에 남고, 다시 읽을
때 `JSON.parse` 가 실패합니다. 텍스트 형식 내보내기에서 실제로 겪었고,
값이 `undefined` 인 키를 건너뛰도록 고쳤습니다.

### 5.11 셸 스크립트

- `SCRIPT_DIR` 은 **스크립트 맨 앞에서** 계산해야 합니다. 나중에 하면
  중간의 `pushd` 때문에 `dirname "."` 이 엉뚱한 곳을 가리킵니다.
- macOS 의 BSD `sed -i` 는 백업 접미사를 요구합니다 (`sedi()` 헬퍼).
- macOS 기본 bash 는 3.2 라 `set -u` + 빈 배열 `"${A[@]}"` 가 오류입니다.
  `${A[@]+"${A[@]}"}` 형태를 씁니다.
- 오류 출력을 `tail` 로 자르면 원인을 못 봅니다. WASM 빌드는 전체 출력을
  로그로 남깁니다.

---

### 5.12 i18n 커버리지 게이트의 사각지대

`web/src/lib/i18n/__tests__/basic-mode-coverage.test.ts` 는 소스를 훑어
UI 가 쓰는 키가 en·es·pt 세 사전에 모두 있는지 봅니다. 상류 CI 가
이걸 돌리므로 **키 하나가 es 나 pt 에 없으면 PR 이 떨어집니다.**

실제로 기존 `apply-text-io.sh` 는 ko 와 en 에만 문구를 넣어서,
`project.exportText` 등 4개가 es·pt 에 비어 있었습니다. 지금은 채웠습니다.

사각지대가 둘 있으니 새 문구를 넣을 때 주의하세요.

- 스캐너 정규식이 `t('키')` 만 찾습니다. **`tp('키', {...})` 는 잡지
  못합니다** (`tp(` 안에는 `t(` 가 없습니다). 치환 자리가 있는 문구는
  전부 `tp()` 라서, 이 모듈의 오류 문구 31개는 상류 게이트에 안 걸립니다.
  그 자리를 `check-textio-i18n.py` 가 메웁니다.
- 키를 변수로 넘기면(도우미 함수로 감싸면) 역시 안 보입니다. 그래서
  머리말 문구도 호출부마다 키를 통째로 적었습니다. 상류 코드가 같은
  이유로 그렇게 씁니다(테스트 파일 주석에 적혀 있습니다).

### 5.13 `nextId` 하위 키는 앱 경로에서 줄어든다

`dedtext.py` 는 `.ded` 의 `nextId` 를 통째로 `EXTRA` 에 실어 왕복에서
보존하지만, 앱 안의 텍스트 경로는 `nextId` 를 `MODELLED` 로 취급해
텍스트에 쓰지 않고 읽을 때 다시 계산합니다. 그래서 `plate`/`quad`/
`connector` 같은 하위 키가 왕복하면 사라집니다.

앱이 어차피 다시 계산하므로 실사용에 문제는 없습니다. **구판에서도
똑같았습니다** — i18n 작업으로 생긴 것이 아닙니다. 두 구현을 맞추려면
`text-format.ts` 의 `MODELLED` 에서 `nextId` 를 빼면 됩니다.

### 5.14 빌드 명령에서 빠뜨리기 쉬운 두 가지

한 번에 두 개를 빠뜨려 두 가지 증상을 동시에 만난 적이 있습니다.

**`--wasm-cache` 를 빠뜨리면** 솔버 없는 배포본이 나옵니다. 빌드는 성공하고
편집기도 열리며, 해석 버튼에서 `WASM solver not initialized` 로 실패합니다.
예전에는 dist 검증이 `BUILD_WASM` 안에 갇혀 있어서 **바로 그 경우에만 침묵**
했습니다. 지금은 `--wasm`, `--wasm-cache`, `--no-wasm` 중 하나를 고르지
않으면 시작조차 하지 않습니다.

**`--commit` 을 빠뜨리면** 그날의 main 을 받습니다. 상류는 자주 움직이고,
패치는 문자열 치환이라 해당 줄이 바뀌면 멈춥니다. 실제로 2026-09-14 의
PR #197 이 `ToolbarProject.svelte` 를 재설계하면서 `apply-text-io.sh` 의
앵커 두 개가 죽었습니다. CSV 버튼은 들여쓰기가 8칸에서 10칸으로 바뀌었고
(마크업은 같은데 공백이 달라 빗나갑니다), `project.openDed` 키는 컴포넌트에서
`project.openTooltip` 으로 바뀌어 사라졌습니다.

### 5.15 `--keep` 작업 폴더의 두 함정

`--commit` 없이 빌드하면 `--depth 1` 얕은 클론이 만들어집니다. 그 폴더를
`--keep` 으로 남긴 뒤 다음번에 `--commit` 을 주면, `fetch --all` 은 깊이를
늘리지 않으므로 **"커밋 없음" 으로 멈춥니다.** 지금은 얕은 클론을 만나면
`fetch --unshallow` 로 넓힙니다.

또 앞선 빌드가 패치 도중 실패했다면 수정된 파일이 남습니다. 패치는 매번
깨끗한 트리를 전제하므로, 재사용 전에 `reset --hard` 와 `clean -fd` 를
합니다. 확실하게 하려면 그냥 `rm -rf ./stabileo-build` 하셔도 됩니다.

### 5.16 반쪽 WASM 캐시

로더가 import 하는 것은 `.wasm` 이 아니라 wasm-pack 이 함께 뽑는 JS 글루
`dedaliano_engine.js` 입니다. 예전 캐시 검사는 `*.wasm` 존재만 봐서, 반쪽
캐시를 조용히 통과시키고 해석 시점에야 드러났습니다. 지금은 `*.js` 도
확인합니다. 온전한 캐시는 이 일곱 개입니다.

```
COMMIT  package.json  README.md
dedaliano_engine.js   dedaliano_engine.d.ts
dedaliano_engine_bg.wasm   dedaliano_engine_bg.wasm.d.ts
```

크레이트 이름은 아직 `dedaliano-engine` 입니다. 앱 이름이 Stabileo 로 바뀐
뒤에도 `engine/Cargo.toml` 은 그대로라, 파일 이름의 `dedaliano` 는 정상입니다.

## 6. 적용한 수정 6가지

### 6.1 단계별 강성법 NaN (원본 버그)

Advanced 의 단계별 설명에서 **7단계 이후 변위와 반력이 전부 NaN** 이었습니다.

`solver-detailed.ts` 가 절점하중을 `{ fx, fy, mz }` 로 읽는데 실제
인터페이스는 `{ fx, fz, my }` 입니다. 2D 솔버가 `(x,y)` → `(x,z)` 로
바뀔 때 이 호출부만 갱신되지 않았습니다.

오류 없이 화면까지 가는 이유는 **방어 장치 3곳이 모두 NaN 을 통과**시키기
때문입니다. `Math.abs(undefined) < 1e-15` 도, LU 분해의 `maxVal < tol` 도
NaN 에서는 false 입니다.

수정: 필드명 교정 + `Number.isFinite()` 검사 + 특이성 검사를 `!(a >= b)`
형태로. 절점하중 회귀 테스트 추가(캔틸레버 `PL³/3EI` 검산, 소수점 8자리
일치). 타입 검사 오류가 5건 → 3건으로 줄었는데, **타입 검사기가 이미
잡고 있었지만 기존 오류에 묻혀 있었던 것**입니다.

정식 해석(Rust 솔버)은 영향 없음. TypeScript 교육용 경로만 해당.

### 6.2 한국어 활성화

§5.7 참조.

### 6.3 텍스트 형식 입출력

그래픽 편집기 없이 모델을 고칠 수 있는 `.txt` 내보내기/가져오기.
프로젝트 패널에 버튼이 추가됩니다.

```
MODEL   name=Frame   mode=2d
NODE       1           0           0
ELEMENT    1  frame      1     2    1    1  -         -
SUPPORT    1  pinned
LOAD    nodal   node=3  case=1  fx=10  fz=0  my=0
```

- 알려진 필드 외의 값은 줄 끝 `key=value` 로 보존 (단면의 `tw`/`tf` 등)
- 설계기준·상세 설정은 파일 끝 `EXTRA` 영역에 보존 → 왕복 무손실
- 잘못 편집하면 행 번호와 함께 오류 안내
- 참조 무결성을 변환 단계에서 검사 (앱 로더는 조용히 거부하기 때문)
- 문구는 전부 `textio.*` 키로 빠져 있고 **en·es·pt·ko 네 언어**가 채워져
  있습니다. 내보낸 파일의 머리말도 그때의 UI 언어로 나옵니다.
  문구를 고칠 곳은 `textio-strings.tsv` 한 곳뿐입니다 (§7 의 (b) 참조).

### 6.4 밝은 배경 PNG

화면이 어두워 인쇄·문서 첨부에 부적합했습니다. **색상과 채도는 두고
명도만 뒤집습니다.** 배경은 밝아지고 부재·글자는 어두워지며, 인장 빨강과
압축 파랑은 중간 명도라 그대로 남습니다. 배경만 예외로 순백 처리.

결과 이미지를 다루므로 2D 와 3D 에 동일하게 통합니다. 3D 는 색이
하드코딩되어 있고 글자가 스프라이트 텍스처라 테마 교체로는 어렵습니다.

참고: PNG 해상도는 2D 가 화면 픽셀 그대로, 3D 는 `devicePixelRatio`
적용. 보고서용으로는 벡터인 **SVG 를 권장**(2D 전용).

---

### 6.5 분리된 구조물 허용

서로 떨어진 두 구조물(예: 똑같은 단순보가 위아래로 놓인 경우)을 해석하면
**"비연결 구조물 … 모든 구간을 연결하세요"** 로 거부당했습니다.

막던 것은 Rust 솔버가 아니라 `solver-service.ts` 의 `prepareSolve2D()`
입니다. 절점 하나에서 너비우선 탐색을 돌려 전부 닿지 않으면 반환합니다.
주석에도 `structure must be a single connected component` 라고 적혀 있어
의도적인 제약이었습니다.

솔버에는 그런 요구가 없습니다. 강성행렬이 블록 대각이 되어 각 블록이
구속되어 있으면 촐레스키가 그대로 통과합니다. `pre_solve_gates.rs` 의
게이트 다섯도 전부 경고일 뿐 해석을 멈추지 않습니다.

수정: 탐색을 모든 절점이 소진될 때까지 돌려 덩어리 목록을 만들고,
덩어리마다 구속 자유도와 반력 평형행렬 랭크를 봅니다. 문제가 있으면
그 절점 번호를 짚어 줍니다. **덩어리가 하나면 아무것도 달라지지 않습니다.**

`prepareSolve2D` 의 기존 검사들은 모든 지점을 한 덩어리로 묶어 계산합니다.
보 A 에 핀+롤러, 보 B 에 지점 없음이면 합계는 3 이라 통과하는데, 지금까지는
연결성 검사가 그 뒤에서 걸러 주고 있었을 뿐입니다. **연결성 검사만 빼면
곧장 솔버까지 가서 `Singular stiffness matrix` 를 받습니다** — 지금보다
나쁜 안내입니다. 두 변경은 반드시 함께 가야 합니다.

3D 경로(`prepareSolve3D`, 1724행 부근)에도 같은 구조가 있습니다. 강의에서
쓰지 않아 손대지 않았습니다.

### 6.6 2D 지점 강제변위 표기

2D 에서 지점 침하를 입력할 때 연직 성분이 `dy` 로 나왔습니다. 앱의 2D
어휘는 `[ux, uz, ry]` 이고 결과표도 `uz`·`rz` 라, y 를 연직으로 오해하기
쉬운 자리였습니다(§5.9 의 축 혼동이 화면에 드러난 것).

저장 필드는 이미 절반쯤 정리되어 있었습니다. `dz` 가 정본이고 `dy` 는 옛
파일용 별칭이며 `canonicalSupportDz()` 가 `dz ?? dy` 로 받습니다. 그런데
**입력 UI 는 여전히 `dy` 에 쓰고 `dy` 에서 읽고** 있었습니다. 한 지점에
둘 다 있으면 해석은 `dz` 를, 화면은 `dy` 를 보여 서로 다른 값을 가리킵니다.

수정: 네 화면(지점 표·속성 패널·선택 패널·지점 도구 옵션)이 `dz` 에 쓰고
`dz ?? dy ?? 0` 으로 읽습니다. 라벨이 아직 `dy` 이던 두 곳도 `dz` 로
바꿨습니다. 옛 파일은 그대로 열립니다.

손대지 않은 것이 둘 있습니다. `uiStore.supportDy` 는 3D 뷰포트도 읽는데
거기서는 `dy` 가 맞는 축입니다(2D 생성 경로는 이미 `presc.dz` 로 씁니다).
i18n 키 이름(`prop.imposedDyTitle` 등)도 그대로입니다 — 문구 자체는 네
언어 모두 "연직" 으로 맞게 쓰여 있어 사용자에게 보이는 것은 같습니다.

---

## 7. 지금 상태와 남은 일

### 끝난 것
- 6개 패치 모두 적용·검증 완료 (고정 커밋에서 체인 전체 통과)
- 3개 OS 배포본 생성 확인 (Windows·Linux 는 학생 PC 실행까지 확인)
- GitHub 업로드 완료 (허용 목록 방식 `.gitignore`)
- **텍스트 입출력의 한국어 하드코딩 제거** — 아래 (b) 참조

### 남은 일

**(a) 상류 PR — NaN 수정만**

`upstream/` 에 준비되어 있습니다. 아직 제출하지 않았습니다.

```bash
gh repo fork lambdaclass/stabileo --clone
cd stabileo && git checkout -b fix/dsm-nodal-load-nan
../upstream/prepare-pr.sh .
git add -A && git commit -F ../upstream/COMMIT_MSG.txt
git push -u origin fix/dsm-nodal-load-nan && gh pr create --web
```

`PR.md` 의 제목·본문을 붙여넣으면 됩니다. CI 는 `web` 에서
`npm run test` 를 돌리는데, 전체 실행에는 WASM 빌드가 선행되어야 합니다.

**(b) 텍스트 입출력의 한국어 하드코딩 — 끝남, 번역 검토만 남음**

`text-format.ts` 의 사용자 문구를 전부 `textio.*` 키로 뺐고 주석도 영어로
바꿨습니다. 키 71개(`textio.*` 67 + 툴바 `project.*` 4)를 **en·es·pt·ko**
네 언어로 채웠고, `apply-text-io.sh` 가 `textio-strings.tsv` 를 읽어 네
사전에 넣습니다. 여러 번 실행해도 중복되지 않습니다.

검증: esbuild 로 구·신 두 판을 나란히 돌려 비교했습니다. 네 언어 모두
파싱 결과·개수·오류 개수가 같고 **내보낸 파일의 주석 아닌 줄은 한 바이트도
다르지 않습니다.** `dedtext.py` 와의 양방향 호환도 확인했습니다.

남은 것은 **es·pt 번역의 원어민 검토** 하나입니다. 상류에 보내기 전에
받아야 합니다. 검토 후에는 `check-textio-i18n.py` 를 한 번 돌리면 됩니다.

**(c) 한국어 번역 상류 기여**

`ko-extra.ts` 를 `ko.ts` 에 병합해 커버리지를 올린 뒤 제안하는 형태가
받아들여질 가능성이 높습니다. 현재 55% 수준이라 그대로는 거절될 수
있습니다 (§5.7 의 개발팀 판단 참조).

**(d) macOS 배포본 실기 확인**

Gatekeeper 해제 절차가 실제 학생 맥에서 잘 도는지 미확인.

---

## 8. 다음 대화에서 확인하면 좋은 것

원본 저장소는 계속 바뀝니다. 작업 전에 다음을 확인하세요.

> **2026-09-14 확인 결과** (상류 HEAD `469bddcc`)
> - NaN 버그는 **아직 상류에 남아 있습니다** (`solver-detailed.ts` 가
>   여전히 `fx, fy, mz`). (a) 의 PR 은 그대로 유효합니다.
> - `OFFERED_LOCALES` 는 여전히 `['es','en','pt']` 입니다.
> - 고정 커밋 이후 `web/src/lib/engine/` 에 40여 커밋이 쌓였지만
>   **패치 4개 모두 최신 main 에 그대로 적용됩니다.** 치환 문자열이
>   하나도 어긋나지 않았습니다.

```bash
# 고정 커밋과 최신 사이의 차이
git log --oneline b1cc418ef424..origin/main -- web/src/lib/engine/

# NaN 버그가 상류에서 이미 고쳐졌는지
grep -n "fx, fy, mz\|fx, fz, my" web/src/lib/engine/solver-detailed.ts

# 한국어가 제공 목록에 들어갔는지
grep -n "OFFERED_LOCALES" web/src/lib/i18n/store.svelte.ts
```

패치 스크립트는 문자열 치환 방식이라, 상류가 해당 줄을 고치면 "찾지
못했습니다" 로 중단됩니다. 그때는 해당 부분만 다시 맞추면 됩니다.
